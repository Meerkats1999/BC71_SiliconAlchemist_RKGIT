from darkflow.net.build import TFNet
import numpy as np
import cv2


def makeBoxAndStuff(original_img, predictions):
    
    return newImage

options = {"model": "cfg/yolo.cfg", "load": "bin/yolo.weights", "threshold": 0.3}

tfnet = TFNet(options)

